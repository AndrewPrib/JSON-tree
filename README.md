1) Для запуска используется форма для командной строки:

python main.py < input.json 

main.py - основной код программы

input.json - файл с входными данными

2) Для подключения библиотек:

pip install -r requirements.txt

